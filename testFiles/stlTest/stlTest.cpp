#include <vector>
#include <iostream>
#include <map>
using namespace std;

int main(){
	std::vector<int> vec;   // declare a vector of int
	vec.push_back(10);      // add some values
	vec.push_back(5);
	vec.push_back(1);
	//here for should trigger Ultisnip
	//2013-09-29 I maped <Ctrl+j>
	for (int i = 0; i < 10; ++i)
	{
		//
	}
}
