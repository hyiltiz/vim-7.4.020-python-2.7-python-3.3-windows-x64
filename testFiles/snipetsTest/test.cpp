#include <vector>
#include <iostream>
#include <map>
using namespace std;

int main(){
	std::vector<int> vec;   // declare a vector of int
	vec.push_back(10);      // add some values
	//2013-09-29 
	//1) writting for then the snip is found by YCM
	//2) I added a function that make ultisnip work ok
	//3) just do Tab or Shift-Tab	
	for (int i = 0; i < 10; ++i)
	{
		//do stuff
	}
}
