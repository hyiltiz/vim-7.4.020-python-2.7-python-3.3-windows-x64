function <- test(alpha,beta){
    coucou <- rnorm(10)
	test <- rnorm(1000)
    plot.ecdf(test);
	a <- unix.time(expr = "");
	p <- rnbinom(17);
}

test <- function(coucou=3,test=4)
{
  #done with snipmate with fun<tab>
}
